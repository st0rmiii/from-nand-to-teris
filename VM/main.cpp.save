#include <iostream>
#include "Parser.h"
#include "CodeWriter.h"

using namespace std;

int main()
{
    string a,oname,iname;
    cin>>a;
    a="E:\\"+a;
    oname=a+".vm";
    iname=a+".hack";
    Parser pro1(oname);
    while(pro1.hasMoreCommands())
    {
        pro1.advance();
    }
    CodeWriter pro2(iname);
    return 0;
}
